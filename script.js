// Inicializa o carrossel
var myCarousel = new bootstrap.Carousel(document.getElementById('carouselExampleFade'), {
    interval: 2000, // Intervalo entre os slides (opcional)
    wrap: true // Define se o carrossel deve voltar ao primeiro slide após o último (opcional)
  });
  